<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#">Logout</a></li>
        
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div>
    <%= link_to "New deposit", new_account_path %> 
</div>

<div class="accounts">
    <% @accounts.each do |account| %>
        <div class="account">
            <h3><%= link_to account.name, account_path(account) %></h3>
            <p><%= account.description %></p>
            <ul class="account-actions">
                <li><%= link_to "Edit", edit_account_path(account) %></li>
                <li><%= link_to "Delete", account_path(account), data: {
                    confirm: "Are you sure?"}, method: :delete %></li>
            </ul>
        </div>
    <% end %>
</div>